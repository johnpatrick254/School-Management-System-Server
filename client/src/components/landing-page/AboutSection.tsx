import { FC } from "react";

interface AboutSectionProps {}

const AboutSection: FC<AboutSectionProps> = ({}) => {
  return (
    <div id="about" className="h-screen pt-12">
      AboutSection
    </div>
  );
};

export default AboutSection;
